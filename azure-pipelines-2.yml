trigger:
  branches:
    include:
      - main

pool:
  name: 'Default'

variables:
  pythonVersion: '3.9.6'
  requirementsFile: 'requirements.txt'
  testFolder: 'test_test1.py'
  testResultsFile: 'test-results.xml'

steps:
  - task: CmdLine@2
    inputs:
     script: 'git clone https://sandracardona187362@dev.azure.com/sandracardona187362/Primer%20Pipeline/_git/Primer%20Pipeline'
    displayName: 'Hacer Checkout del repositorio'


  - task: UsePythonVersion@0
    inputs:
      versionSpec: '$(pythonVersion)'
      addToPath: true
    displayName: 'Configurar Python $(pythonVersion)'

  - script: |
      python -m pip install --upgrade pip
      pip install -r $(requirementsFile)
    displayName: 'Instalar dependencias'

  - script: |
      echo "Ejecutando pruebas unitarias en $(testFolder)"
      pytest $(testFolder) --junitxml=$(testResultsFile)
    displayName: 'Ejecutar pruebas unitarias'

  - task: PublishTestResults@2
    inputs:
      testResultsFiles: '**/$(testResultsFile)'
      testResultsFormat: 'JUnit'
    displayName: 'Publicar resultados de pruebas'